name = "job-application-automation"
compatibility_date = "2026-08-13"

# Cloudflare Pages — tells the build where to find the compiled output
pages_build_output_dir = "frontend/dist"

[vars]

[observability]
enabled = true
