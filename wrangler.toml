name = "job-application-automation"
compatibility_date = "2026-08-13"

# Cloudflare Pages configuration
# The build runs from `frontend/` (set as Root Directory in the dashboard)
# so the output directory is just `dist` relative to `frontend/`
pages_build_output_dir = "dist"

# Security headers
[vars]

[observability]
enabled = true
