name = "job-application-automation"
compatibility_date = "2026-08-13"

# Cloudflare Pages — the build runs inside frontend/ (Root Directory)
# so the Vite output goes to frontend/dist/
# This path is relative to the repo root where this file lives
pages_build_output_dir = "frontend/dist"

[vars]

[observability]
enabled = true
